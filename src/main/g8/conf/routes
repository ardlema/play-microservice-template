# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Cookbook routes
GET     /receta/:descripcion        controllers.CookBookController.getReceta(descripcion: String)

POST    /receta                     controllers.CookBookController.createReceta()
